<nav class="navbar navbar-expand-lg navbar-light sticky-top bg-white shadow-sm">
    <div class="container">
        <a href="/">
            <span class="navbar-brand text-primary text-small">
                <img class="mx-auto" src="../assets/images/Logo2.png" width="50" alt="">
                EatMod
            </span>
        </a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarContent">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div id="navbarContent" class="collapse navbar-collapse">
            <ul class="navbar-nav ml-auto">
                <% if (!user) { %>
                <li class="nav-item">
                    <a class="nav-link active" href="/login"><span style="font-size: 18px"><i class="fas fa-user-circle text-primary"></i></span> เข้าสู่ระบบ</a>
                </li>
                <% } else { %>
                <li class="nav-item dropdown shadow-sm border hover-1">
                    <a class="nav-link active dropdown-toggle px-2" href="#" data-toggle="dropdown">
                        <span style="font-size: 18px"><i class="fas fa-user-circle text-primary"></i> <%= user.display_name %></span>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right">
                        <a class="dropdown-item" href="#">สวัสดีจ้า อยากนอนแล้ว</a>
                        <div class="dropdown-divider"></div>
                        <h6 class="dropdown-header">อื่นๆ</h6>
                        <a class="dropdown-item" href="#"><i class="fas fa-envelope"></i> รายงานผู้พัฒนา</a>
                        <a class="dropdown-item" href="#"><i class="fas fa-cog"></i> การตั้งค่า</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/logout"><i class="fas fa-sign-out-alt"></i> ออกจากระบบ</a>
                    </div>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>